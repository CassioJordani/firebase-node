<!DOCTYPE html>
<html>
<head>
    <title>Listar Cursos</title>
    <link rel="stylesheet" href="/public/css/styles.css">
</head>
<body>
    <h1>Lista de Cursos</h1>
    <ul>
        <% cursos.forEach(curso => { %>
            <li>
                <%= curso.disciplina %> - <%= curso.professor %> - <%= curso.turno %> - <%= curso.carga_horaria %> horas
                <a href="/edit-curso/<%= curso.id %>">Editar</a>
                <form action="/delete-curso/<%= curso.id %>" method="post" style="display:inline;">
                    <button type="submit">Deletar</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <a href="/add-curso">Adicionar Curso</a>
</body>
</html>
